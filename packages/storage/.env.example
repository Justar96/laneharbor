# Storage Service Configuration

# gRPC Server
STORAGE_GRPC_HOST=0.0.0.0
STORAGE_GRPC_PORT=50051

# Health Check Server (Railway uses PORT for health checks)
# PORT=8080

# Google Cloud Storage Configuration
# ==================================

# Required: GCP Project ID
GOOGLE_CLOUD_PROJECT_ID=your-gcp-project-id

# Required: GCS Bucket Name
GCS_BUCKET_NAME=laneharbor

# Authentication Method 1: Service Account Key File (recommended for local development)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json

# Authentication Method 2: Service Account Key as JSON string (recommended for Railway)
# GCS_SERVICE_ACCOUNT_KEY={"type":"service_account","project_id":"..."}

# Authentication Method 3: Default Application Credentials (if running on GCP)
# No additional configuration needed - will use metadata service

# Railway Deployment Configuration
# ===================================
# For Railway deployment, you need these environment variables:
# 
# GOOGLE_CLOUD_PROJECT_ID=your-gcp-project-id
# GCS_BUCKET_NAME=laneharbor (or your preferred bucket name)
# GCS_SERVICE_ACCOUNT_KEY={"type":"service_account",...} (full JSON as string)
# 
# To get the service account key:
# 1. Go to Google Cloud Console
# 2. Navigate to IAM & Admin > Service Accounts
# 3. Create a new service account or select existing one
# 4. Add roles: "Storage Admin" or "Storage Object Admin"
# 5. Create a JSON key and copy the entire content
# 6. Paste the JSON content as the GCS_SERVICE_ACCOUNT_KEY value in Railway
# 
# Note: The bucket will be created automatically if it doesn't exist

