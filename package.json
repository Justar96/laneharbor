{
  "name": "laneharbor",
  "private": true,
  "packageManager": "npm@10.9.2",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "postinstall": "npm run -ws postinstall --if-present",
    "build": "npm run -ws build",
    "dev": "concurrently -n backend,frontend -c green,cyan \"npm --workspace @laneharbor/backend run dev\" \"npm --workspace @laneharbor/frontend run dev\"",
    "start": "concurrently -n backend,frontend -c green,cyan \"npm --workspace @laneharbor/backend run start\" \"npm --workspace @laneharbor/frontend run start\"",
    "clean": "git clean -xdf -e .env -e .env.*",
    "typecheck": "npm run -ws typecheck --if-present"
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "typescript": "^5.5.4"
  },
  "engines": {
    "node": ">=20"
  }
}
